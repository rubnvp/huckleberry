{
  "name": "huckleberry",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Joshua Comeau",
  "scripts": {
    "start": "concurrently npm:watch:*",
    "watch:live-server": "live-server --port=9000 --host=localhost --no-css-inject .",
    "watch:tailwind-css": "tailwindcss -i ./input.css -o ./output.css --watch"
  },
  "dependencies": {
    "live-server": "1.2.1"
  },
  "engines": {
    "npm": ">5"
  },
  "devDependencies": {
    "concurrently": "^6.5.1",
    "tailwindcss": "^3.0.8"
  }
}
